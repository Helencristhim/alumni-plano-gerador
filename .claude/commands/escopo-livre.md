---
description: Remove a trava de escopo (kids-teens/adulto). Volta ao modo sem restrição de modelo.
---

# /escopo-livre — remove a trava de escopo

1. Remova o arquivo `.claude/scope.lock` (`rm -f .claude/scope.lock` via Bash).
2. Confirme ao usuário: "Trava de escopo removida — sem restrição de modelo. As regras do
   `CLAUDE.md` continuam valendo (adulto ativo/legado segue intocável sem OK explícito)."
